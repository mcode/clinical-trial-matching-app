import { ReactElement } from 'react';
import { GetServerSideProps } from 'next';

const ResultsDetailsPage = (): ReactElement => null;

export default ResultsDetailsPage;

export const getServerSideProps: GetServerSideProps = async () => {
  return { props: {} };
};
